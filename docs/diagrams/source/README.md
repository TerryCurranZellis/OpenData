# PlantUML source directory

This directory contains all authoritative OpenData PlantUML files. Keep the directory flat so the renderer produces predictable matching filenames in `../generated`.

Do not place rendered images here. Do not link Markdown illustrations to `.puml` files; link to `../generated/<diagram>.svg`.
