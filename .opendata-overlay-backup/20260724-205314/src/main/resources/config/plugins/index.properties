# Installed Phase 1 plugin definitions.
#
# Keep this list in sync with config/plugins/<plugin-id>.properties.
# The explicit index works both from an IDE and from a packaged JAR.
plugins=ofgem,openmeteo
