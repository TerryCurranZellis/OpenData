/**
 * Initialisation and typed configuration for the Octopus adjustment plugin.
 *
 * <p>This package validates the {@code octopus-adjustment} plugin definition,
 * account-number filename prefix, input path, working path and archive path,
 * and coordinates the plugin processing stages.</p>
 *
 * @since 3.1.0
 */
package com.towermarsh.opendata.plugin.octopusadjustment.initialise;
